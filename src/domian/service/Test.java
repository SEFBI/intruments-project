package domian.service;

import domian.exceptions.NotFoundInstrumentException;

import javax.sound.midi.MidiUnavailableException;


public class Test {
    public static void main(String[] args) throws MidiUnavailableException, NotFoundInstrumentException {
//        MidiPlayer player = new MidiPlayer();
//
//        InstrumentElement piano = InstrumentFactory.getInstrument(TypeInstrument.VIOLIN, player);
//        //piano.playNotes(Arrays.asList(Note.DO, Note.RE, Note.MI, Note.FA, Note.SOL, Note.SI));
//        //piano.play(0);
//        piano = InstrumentFactory.getInstrument(TypeInstrument.GUITAR, player);
//        piano.playNotes(Arrays.asList(Note.DO, Note.RE, Note.MI, Note.FA, Note.SOL, Note.SI));
//        piano.play(0);
////
////        piano = InstrumentFactory.getInstrument(TypeInstrument.ELECTRIC_GUITAR,player);
////        piano.playNotes(Arrays.asList(Note.DO,Note.RE, Note.MI, Note.FA,Note.SOL,Note.SI));
////
////        piano= InstrumentFactory.getInstrument(TypeInstrument.TRUMPET,player);
////        piano.playNotes(Arrays.asList(Note.DO,Note.RE, Note.MI, Note.FA,Note.SOL,Note.SI));
//
//        player.close();
    }
}
